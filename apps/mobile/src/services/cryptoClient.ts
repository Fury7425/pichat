import {
  createIdentity,
  createPreKeys,
  ensureSession,
  encryptFor,
  decryptFrom,
  exportRecoveryKit,
  importRecoveryKit
} from '@pichat/crypto';

export const cryptoClient = {
  createIdentity,
  createPreKeys,
  ensureSession,
  encryptFor,
  decryptFrom,
  exportRecoveryKit,
  importRecoveryKit
};
